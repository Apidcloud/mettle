{
  "Flow declaration": {
    "prefix": "flow",
    "description": "Declare a reusable Flow function",
    "body": [
      "flow ${1:name}(${2}) {",
      "    ${3:return null}",
      "}"
    ]
  },
  "Expression-bodied flow": {
    "prefix": "flowexpr",
    "description": "Declare a concise reusable Flow function",
    "body": [
      "flow ${1:name}(${2}) = ${3:http.get(\"/path\")}"
    ]
  },
  "Anonymous workflow": {
    "prefix": "flowblock",
    "description": "Declare an anonymous multi-operation flow",
    "body": [
      "flow {",
      "    ${1:return http.get(\"/path\")}",
      "}"
    ]
  },
  "Runnable main flow": {
    "prefix": "flowmain",
    "description": "Declare the program entry flow",
    "body": [
      "flow main() {",
      "    ${1:return null}",
      "}"
    ]
  },
  "Namespace declaration": {
    "prefix": "namespace",
    "description": "Declare this file's namespace and an optional dependency",
    "body": [
      "namespace ${1:name}",
      "use namespace ${2:core}"
    ]
  },
  "Assertion": {
    "prefix": "assert",
    "description": "Assert that an expression is true",
    "body": [
      "assert(${1:response.status == 200})"
    ]
  },
  "HTTP context": {
    "prefix": "flowcontext",
    "description": "Declare an HTTP context",
    "body": [
      "context ${1:api} {",
      "    defaults http {",
      "        baseUrl: \"${2:https://postman-echo.com}\"",
      "        timeout: ${3:5s}",
      "    }",
      "}"
    ]
  },
  "HTTP GET": {
    "prefix": "httpget",
    "description": "Send an HTTP GET request",
    "body": [
      "http.get(\"${1:https://postman-echo.com/get}\")"
    ]
  },
  "HTTP JSON POST": {
    "prefix": "httppost",
    "description": "Send an HTTP POST request with a JSON body",
    "body": [
      "http.post(\"${1:https://postman-echo.com/post}\") {",
      "    json: {",
      "        ${2:name}: ${3:\"value\"}",
      "    }",
      "}"
    ]
  }
}

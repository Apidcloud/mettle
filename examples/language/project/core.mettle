namespace core

context base {
    baseUrl: env("API_URL")

    defaults http {
        baseUrl: baseUrl
        timeout: 10s
    }
}

context api {
    use context base

    defaults http {
        headers: {
            "Accept": "application/json"
        }
    }
}
